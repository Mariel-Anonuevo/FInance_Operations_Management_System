# Build stage
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src
COPY FOMS.Proxy/FOMS.Proxy.csproj FOMS.Proxy/
RUN dotnet restore FOMS.Proxy/FOMS.Proxy.csproj
COPY . .
RUN dotnet publish FOMS.Proxy/FOMS.Proxy.csproj -c Release -o /app/publish

# Runtime stage
FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime
WORKDIR /app
COPY --from=build /app/publish .
EXPOSE 8080
ENV ASPNETCORE_URLS=http://+:8080
ENTRYPOINT ["dotnet", "FOMS.Proxy.dll"]
